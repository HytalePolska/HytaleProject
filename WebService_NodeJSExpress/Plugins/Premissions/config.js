

let config = {
  P_MaxSize :10,
  P_MinSize: 3,
};

module.exports = config;


/////////// dowland allplayers +  premissions 
//////////  dowland allcomands + premissoins
////////////for(var p in PrePlayer)
              // p.addPremissions(List<string> premmission);